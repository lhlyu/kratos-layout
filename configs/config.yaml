server:
  http:
    addr: 0.0.0.0:8000
    timeout: 30s
  grpc:
    addr: 0.0.0.0:9000
    timeout: 30s
data:
  database:
    driver: mysql
    source: "${MYSQL_BASE_URI}"
  redis:
    addr: "${REDIS_BASE_URI}"
    read_timeout: 1s
    write_timeout: 1s
